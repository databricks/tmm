# Job code (Step 5)

This folder holds the **Lakeflow Job** generated by Genie Code in
[Step 5](../../docs/50-job.md): the multi-task job that runs the OpenSky SDP pipeline on a
schedule, then a notebook, with retry and a failure notification.

Save the job definition Genie Code produces here (e.g. the job YAML/JSON or a Databricks Asset
Bundle) so readers have a copy-paste reference. It will be filled in during the live-test phase
against a real workspace.
