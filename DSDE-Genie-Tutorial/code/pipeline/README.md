# Pipeline code (Step 4)

This folder holds the **Spark Declarative Pipeline** generated by Genie Code in
[Step 4](../../docs/40-pipeline.md).

Save the pipeline source Genie Code produces here (e.g. `opensky_pipeline.sql` or
`opensky_pipeline.py`) so readers have a copy-paste reference. It will be filled in during the
live-test phase against a real workspace.
