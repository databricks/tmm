# yaml-language-server: $schema=./bundle_config_schema.json

# Gourmet-Snack-Corp

variables:
  prod_warehouse_id:
    description: define prod warehouse id
    default: 4cc49c92ab107b7a

  catalog_name:
    description: "Catalog name for the pipeline"
    default: demos1
  schema_name:
    description: "Schema name for the pipeline"
    default: gourmet-snacks


bundle:
  name: gourmet-snack-corp

include:
  - resources/*.yml


#permissions:
#  - level: CAN_VIEW
#    group_name: marketing
#  - level: CAN_MANAGE
#    user_name: frank.munz@databricks.com
#  - level: CAN_RUN
#    service_principal_name: 123456-abcdef

targets:

  # The 'dev' target, for development purposes. This target is the default.
  dev:
    # We use 'mode: development' to indicate this is a personal development copy:
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default
    # - The 'development' mode is used for Delta Live Tables pipelines
    mode: development
    default: true
    workspace:
      host: https://e2-dogfood.staging.cloud.databricks.com


  
  prod:
    
    # We use 'mode: production' enables strict verification of the settings below.
    # mode: production
    # 'run_as' must be set for all jobs when using 'mode: production'
    mode: development
    
    workspace:
      host: https://dbc-34e8a5a4-09d1.cloud.databricks.com/      
  
      root_path: /Users/frank.munz@databricks.com/dab_${bundle.name}
      
    #run_as:
      # We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      #user_name: frank.munz@databricks.com
