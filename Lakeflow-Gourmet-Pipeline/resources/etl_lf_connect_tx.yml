# yaml-language-server: $schema=../bundle_config_schema.json

resources: 
  pipelines:
    lf-connect-tx:
      name: lf-connect-tx
      root_path: ../src/lf-connect-tx
      libraries:
        - glob:
            include: ../src/lf-connect-tx/transformations/**
      serverless: true
      development: false
      catalog: ${var.catalog_name}
      schema: ${var.schema_name}
      photon: true
      channel: PREVIEW