# yaml-language-server: $schema=../bundle_config_schema.json

resources:
  dashboards:
    gourmet_dashboard:
      display_name: "Gourmet-Pipeline: Realtime Data + genAI + yummy food"
      file_path: ../src/aibi_dashboard.json
      warehouse_id: ${var.prod_warehouse_id}
      